name: Shell Commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: eccho "Hello World!!"
      - name: Multiline Script
        run: |
          node -v
          npm -v